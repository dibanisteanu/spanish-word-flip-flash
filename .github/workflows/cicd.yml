name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container:
      image: node:22-alpine
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload build (optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            dist
            build
          if-no-files-found: ignore

  test_unit:
    name: Unit Tests (Vitest)
    runs-on: ubuntu-latest
    needs: build
    container:
      image: node:22-alpine
    steps:
      - uses: actions/checkout@v4
      - name: Use npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Install deps
        run: npm ci
      - name: Run Vitest
        run: npx vitest run --reporter=verbose

  test_integration:
    name: Integration Tests (Playwright)
    runs-on: ubuntu-latest
    needs: build
    container:
      image: mcr.microsoft.com/playwright:v1.54.2-jammy
    steps:
      - uses: actions/checkout@v4
      - name: Use npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Install deps
        run: npm ci
      - name: Run Playwright integration tests
        run: npx playwright test

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    needs: [test_unit, test_integration]
    container:
      image: mcr.microsoft.com/playwright:v1.54.2-jammy
    env:
      E2E_BASE_URL: https://spanish-cards.netlify.app/
    steps:
      - uses: actions/checkout@v4
      - name: Use npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Install deps
        run: npm ci
      - name: Run Playwright E2E
        run: npx playwright test

      # Publish HTML report (equivalent to publishHTML in Jenkins)
      - name: Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-playwright-html-report
          path: reports-e2e/html/
          if-no-files-found: ignore

      # Publish JUnit test results (equivalent to junit in Jenkins)
      - name: Upload E2E JUnit XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-junit
          path: reports-e2e/junit.xml
          if-no-files-found: ignore

  deploy:
    name: Deploy (mock)
    runs-on: ubuntu-latest
    needs: e2e
    container:
      image: alpine:latest
    steps:
      - name: Mock deployment
        run: echo 'Mock deployment was successful!'
